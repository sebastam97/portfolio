import styled from 'styled-components';

export const TitlePixel = styled.h1`
  font-size: 2.5rem;
  font-weight: bold;
  text-align: center;
  font-family: var(--font-press-start-2p);
  text-shadow: 2px 2px 0 #000;
  letter-spacing: -1px;
  margin-bottom: 0.5rem;
  color: #4ade80;
`;
